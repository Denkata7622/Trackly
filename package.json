{
  "name": "trackly",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend -- --hostname 0.0.0.0 --port 3000\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "clean": "node ./scripts/clean.mjs",
    "reset": "npm run clean && npm run setup",
    "test": "node --test --experimental-strip-types \"backend/tests/**/*.test.ts\" \"frontend/tests/**/*.test.ts\"",
    "test:backend": "node --test --experimental-strip-types \"backend/tests/**/*.test.ts\"",
    "check:backend": "node scripts/backend-validate.mjs && npm run test:backend"
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "file-type": "^21.3.0"
  }
}
